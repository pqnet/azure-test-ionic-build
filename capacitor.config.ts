import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'azure-test-ionic-build',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
